#pragma once
// Copia este archivo a `secrets.h` (git-ignored) y rellena tus valores.
// NUNCA commitees secrets.h.
//
// WiFi: se configura desde el PORTAL del ESP32. Opcional (pruebas): descomenta
// para hardcodear una red 2.4 GHz; si está definida, se usa antes del portal.
// #define WIFI_SSID      "MiRed2G"
// #define WIFI_PASSWORD  "MiClave"

// Supabase — usar la PUBLISHABLE key (rol anon). La SECRET/service_role JAMÁS.
#define SUPABASE_URL        "https://xxxx.supabase.co"
#define SUPABASE_ANON_KEY   "sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx"

// ── Identidad POR-UNIDAD (device_id + token son un par) ──────────────────────
// Deben coincidir con la fila de ESTA unidad en devices / device_tokens de
// Supabase. Cambiá AMBOS al flashear otra unidad. Ej.: ...0001, ...0002, …
#define DEVICE_ID           "00000000-0000-0000-0000-0000000000XX"

// Token propio del device (header x-device-token) para consumir comandos.
// Debe existir en la tabla device_tokens de Supabase (I3.2).
#define DEVICE_TOKEN        "dvt_xxxxxxxxxxxxxxxxxxxxxxxx"
